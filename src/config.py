class Directories:
    root = ".."

    data = root + "/" + "data"
    processed_data = data + "/" + "processed"
    raw_data = data + "/" + "raw"

    outputs = root + "/" + "outputs"
    charts = outputs + "/" + "charts"
    json_outputs = outputs + "/" + "json"
